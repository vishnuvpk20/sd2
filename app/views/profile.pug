doctype html
html
  head
    title= title
  body
    h1= title

    table(border="1")
      thead
        tr
          each key in Object.keys(results[0])
            th= key
      tbody
        each row in results
          tr
            each val in row
              td= val
